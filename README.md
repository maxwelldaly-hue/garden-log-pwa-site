# Dad's Garden — moved

This site used to serve the compiled client for a private garden-log PWA. The app is now
served from its own host, so both pages here are redirects and nothing else. Nothing is
hand-edited — every file is regenerated by the private repo's `build/build-static.js`.

- `/` — redirects to the garden
- `/sandbox/` — redirects to the demo
