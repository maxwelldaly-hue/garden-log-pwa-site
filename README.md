# Dad's Garden — compiled client

Auto-published, compiled output of a private garden-log PWA (static client for a Google
Apps Script backend). Nothing here is hand-edited — every file is regenerated by the
private repo's `build/build-static.js` on deploy. The app itself is PIN-gated.

- `/` — the garden
- `/sandbox/` — the friends' sandbox copy
